This directory stores user profile data as JSON files so the app can read and render them directly.

- Each user profile is a JSON file named after the user id, for example temp.json
- The credentials index file maps each user id to username, password, and profile path
- New sign-ups create a new profile JSON file and add an entry to the credentials index
